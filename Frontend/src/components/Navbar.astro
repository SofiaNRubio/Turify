---

---

<nav style="display: flex; justify-content: space-between; align-items: center; padding: 1rem;">
    <div style="font-weight: bold; font-size: 1.5rem;">TURIFY</div>
    <div style="display: flex; gap: 1.5rem;">
        <a href="/">Principal</a>
        <a href="/indicaciones">Indicaciones</a>
        <a href="/emergencias">Emergencias</a>
        <a href="/servicios">Servicios</a>
        <a href="/hospedaje">Hospedaje</a>
        <a href="/informacion-util">Información Util</a>
    </div>
</nav>
<!--
<nav>
    <a href="/">Inicio</a>
    <a href="/empresas">Empresas</a>
    <span id="auth-section"></span>

    <script type="module" is:inline>
        import { Clerk } from "@clerk/clerk-js";

        const clerk = new Clerk(import.meta.env.PUBLIC_CLERK_FRONTEND_API);
        await clerk.load();

        const authSection = document.getElementById("auth-section");

        const user = await clerk.user;

        if (user) {
            const logoutBtn = document.createElement("button");
            logoutBtn.textContent = "Cerrar sesión";
            logoutBtn.onclick = () => clerk.signOut();
            authSection.appendChild(logoutBtn);
        } else {
            const loginBtn = document.createElement("button");
            loginBtn.textContent = "Iniciar sesión";
            loginBtn.onclick = () => clerk.openSignIn();
            authSection.appendChild(loginBtn);
        }
    </script>
</nav>
-->
